#include <stdio.h>

main()
{

  float x;
  float y;

  // Number One
  x = (4 + 2) * 5;
  printf("Answer to Number One is: %f \t\n", x);

  // Number Two
  x = (12 + 6) / 5;
  printf("Answer to Number Two is: %f \t\n", x);

  // Number Three
  y = (4 + 3) / 4;
  printf("Answer to Number Three is: %f \t\n", y);

  // Number Four
  x = 4 + 5 * (7 / 3);
  printf("Answer to Number Four is: %f \t\n", x);

  // Number Five
  x = (4 + 3) * 11.5;
  printf("Answer to Number Five is: %f \t\n", x);

  // Number Six
  x = 6 / 7 * 31.0;
  printf("Answer to Number Six is: %f \t\n", x);

  // Number Seven
  x = 31.0 * 6 / 7;
  printf("Answer to Number Seven is: %f \t\n", x);

  // Number Eight
  x = 20.0 / 3.0 * 5.0;
  printf("Answer to Number Eight is: %f \t\n", x);

  // Number Nine
  x = 30 / 4 * 4.0;
  printf("Answer to Number Nine is: %f \t\n", x);

  // Number Ten
  x = 30 * 4.0 / 4;
  printf("Answer to Number 10 is: %f \t\n", x);

}
